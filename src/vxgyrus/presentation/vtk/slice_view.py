"""VTK 2D slice view widget.

Put here:
- QVTKRenderWindowInteractor integration (if using Qt+VTK)
- renderer setup (camera, background, interactions)
- API:
  - set_volume(volume)
  - set_mask(mask)
  - set_slice(k)
  - set_overlay_alpha(a)
"""
